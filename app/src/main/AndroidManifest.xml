<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studenttask">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentInfoModify"></activity>
        <activity android:name=".GetPictureDemo2" />
        <activity android:name=".ImageDemo" />
        <activity android:name=".GetPictureDemo" />
        <activity android:name=".FragmentStaticDemo" />
        <activity
            android:name=".PersonalCenter"
            android:label="个人信息" /> <!-- android:label="学生信息展示界面2" -->
        <activity
            android:name=".ListStudents2"
            android:theme="@style/TitleTheme" />
        <activity
            android:name=".Login"
            android:label="欢迎来到登录界面" />
        <!--
<activity
            android:name=".ListStudents"
            android:label="学生信息展示" />
        <activity android:name=".JudgeStudent" />
        -->
        <!-- 以下是设置第一启动项 -->
        <activity
            android:name=".Regist"
            android:label="欢迎来到注册界面">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>